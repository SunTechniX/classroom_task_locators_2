# 🏠 Домашнее задание: локаторы  
**🎯 Цель**: Научиться самостоятельно подбирать и сравнивать CSS и XPath локаторы для реальных элементов.

---

## 📌 Задание 1: Сравнение CSS и XPath

**Страница**: [https://demoqa.com/buttons](https://demoqa.com/buttons)

Выберите **три разных элемента**:
- Кнопка **"Double Click Me"**
- Кнопка **"Right Click Me"**
- Кнопка **"Click Me"**

Для **каждого** элемента:
1. Найдите его в DevTools.
2. Запишите **один CSS-селектор** и **один XPath**.
3. Сохраните их в файл `task_01.py` в виде строк:

```python
DOUBLE_CLICK_CSS = "..."
DOUBLE_CLICK_XPATH = "..."

RIGHT_CLICK_CSS = "..."
RIGHT_CLICK_XPATH = "..."

CLICK_ME_CSS = "..."
CLICK_ME_XPATH = "..."
```

✅ Локаторы должны находить ровно один элемент и быть работоспособными.

## 📌 Задание 2: Ловушка локаторов

**Страница**: [https://demoqa.com/links](https://demoqa.com/links)

На странице есть несколько ссылок с одинаковым классом a[href] (например, "Home", "Created", "No Content"...).

Ваша задача — найти вторую ссылку (текст "Home" под заголовком "Following links will open new tab").

Запишите в task_02.py:

```python
SECOND_LINK_CSS = "..."   # должен найти только вторую ссылку
SECOND_LINK_XPATH = "..." # должен найти только вторую ссылку
```

❗ Локатор .link или //a — неприемлем, так как находит много элементов.

✅ Используйте :nth-child(), :nth-of-type(), позиционные XPath (//...)[2] и т.п.

## 💡 Советы
- Используйте Playwright Inspector или DevTools → Elements → Copy selector / Copy XPath
- Проверяйте локаторы в консоли браузера: document.querySelectorAll('...') или $x('...')
- Избегайте id, если он динамический (например, button-12345)
